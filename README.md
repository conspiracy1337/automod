Set up a venv with "python -m venv .venv"

Activate on linux with "source .venv/bin/activate"
or on Windows with ".venv\Scripts\activate"

Install dependencies with "pip install -r requirements.txt"

Create .env file and write BOT_TOKEN=yourtoken into it

Start bot with "python bot.py"
